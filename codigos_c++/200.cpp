#include<bits/stdc++.h>
using namespace std;

vector<int> fibo(int n) {
    vector<int> a;
    if (n==0) {
        a.push_back(0);
    } 
}

int main() {
    vector<int> vec;
    while (true) {
        int a;
        cin >> a;
        vec.push_back(a);
    }
    for (int i=0 ; i<vec.size() ; i++) {
        
    }
}