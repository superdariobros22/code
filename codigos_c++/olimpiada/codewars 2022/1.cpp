#include<bits/stdc++.h>
using namespace std;

int main() {
    string a, b, c, d;
    getline(cin, a);
    getline(cin, b);
    getline(cin, c);
    getline(cin, d);
    cout << a << " is the " << b << " of CodeWars. We " << c << " you " << d << " and have fun!";
}