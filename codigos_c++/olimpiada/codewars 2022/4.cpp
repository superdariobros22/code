#include<bits/stdc++.h>
using namespace std;

int main() {
    int a, b, c, d, e;
    string x, y;
    cin >> a >> x >> b >> c >> y >> d >> e;
    cout << x << " must win " << a-b-e+1 << " matches";
}