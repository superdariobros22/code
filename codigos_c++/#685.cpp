#include<bits/stdc++.h>
using namespace std;
int main() {

    vector<string> vec;

    while(true) {

        int a, b, c, d;

        cin >> a >> b >> c >> d;

        if (a==0 && b==0 && c==0 && d==0) {
            
        }
    }
    
}