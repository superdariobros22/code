#include<bits/stdc++.h>
using namespace std;

int main() {
    int a;
    cin >> a;

    for (int i=0 ; i<a ; i++) {
        string s;
        cin >> s;
        int max=0, tes=0;
        for (int j=0 ; j<s.size() ; j++) {
            if (s[j]=='T') {
                tes++;
            }
        }
    }
}